[project]
name = "hermes-micromeet"
version = "0.1.2"
description = "MicroMeet coordination tools and gateway adapter for Hermes Agent"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
